
things to implement:

- ordering layer for each shader.
- hyper optimizing each method.
- create "front end" inspector interface.
	// I want to make a couple different nodes with different properties
	// This one will be the legacy shaders node.

when the node is ready:

- create the editor tool script.
- configure the plugin.
- add documentation and comments to all variables and to the node itself.
